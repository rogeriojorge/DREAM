This folder contains the reviewer-facing figure for PR2.

Asset:
- `pr2_kernel_parity.png`: no-bootstrap smoke comparison between the package-backed frontend and the legacy cache-backed path.

Suggested caption:

```text
The package-backed frontend reproduces the same no-bootstrap smoke behavior as the legacy cache-backed path on the QA case.
```

Suggested insertion point:
- after the opening summary and before "What this PR adds"
