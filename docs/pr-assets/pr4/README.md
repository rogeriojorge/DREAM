This folder contains the reviewer-facing figure for PR4.

Asset:
- `pr4_requested_vs_resolved_grid.png`: requested versus resolved DESC equilibrium grids for the QA case.

Suggested caption:

```text
The frontend now makes automatic DESC grid upgrades visible and pairs them with a clearer provider-first `setStellarator(..., provider=...)` workflow.
```

Suggested insertion point:
- after the "What this PR adds" section and before "Why this layer matters"
